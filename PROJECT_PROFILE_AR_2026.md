# نبذة تعريفية تقنية
## استوديو إعلانات الملابس

**نوع المشروع:** تطبيق ويب شخصي وتعليمي لإنشاء إعلانات الملابس باللغة العربية.  
**النطاق:** أدوات تصميم وإخراج إعلان محلياً من صورة قطعة ملابس؛ ليس متجراً إلكترونياً ولا خدمة مدفوعات ولا منصة تجارية عامة.  
**الإصدار الموثق:** `manus-webdev://1e2c8e1e` — أغسطس 2026.

## ما هو التطبيق؟

«استوديو إعلانات الملابس» هو تطبيق ويب متوافق مع اتجاه الكتابة من اليمين إلى اليسار، يتيح للمستخدم اختيار صورة ملابس من الهاتف، قص الجزء المطلوب، إزالة الخلفية محلياً، وإدخال بيانات الإعلان الأساسية مثل اسم المنتج والسعر والخصم. يرسم التطبيق إعلاناً نهائياً بواسطة Canvas داخل قالب ثابت قابل للتخصيص، ثم يسمح بحفظه PNG أو مشاركته مع نص تسويقي مناسب لواتساب.

تستهدف التجربة الهاتف أولاً: أزرار كبيرة، مراحل مختصرة، حفظ محلي للمسودات، وإعدادات اختيارية لا توقف إنشاء الإعلان. ويمكن تجهيز إعلان فردي أو دفعة تصل إلى عشر صور مع بيانات موحّدة أو بيانات مستقلة لكل قطعة.

## رحلة المستخدم

| المرحلة | ما يفعله المستخدم | ما يحدث داخل التطبيق |
|---|---|---|
| إنشاء | يختار إعلاناً فردياً أو دفعة | يحضّر التطبيق مساحة عمل ملائمة للمسار |
| رفع ومراجعة | يختار صورة أو يلتقطها ثم يقصها عند الحاجة | تُقرأ الصورة بمسار متسامح مع اختلاف مزودي ملفات Android |
| تجهيز | يضغط إنشاء الإعلان | تعمل إزالة الخلفية المحلية ثم يُرسم القالب هندسياً |
| النتيجة | يراجع الإعلان والنص | يحفظ PNG أو يشاركه أو يعود للتعديل أو يمسح الجلسة |
| إعدادات | يضبط المقاس والشعار والتذييل والشارات والعناصر | تحفظ خيارات القالب محلياً كي تستخدم في الإعلانات اللاحقة |
| دفعات | يرفع من 1 إلى 10 صور | يعالجها بطابور محدود التوازي مع حفظ واستعادة مؤقتة |

## الوظائف المنفذة

| المجال | الوظائف الحالية |
|---|---|
| تصميم الإعلان | محرك Canvas هندسي بخمسة مقاسات: عمودي، مربع، قصة، واتساب، وأفقي |
| الصور | قص حر قابل للسحب والتحجيم، وقراءة متحملة لصور معرض Android والكاميرا |
| إزالة الخلفية | محلية في المتصفح باستخدام U2NetP وONNX Runtime Web، مع Cache Storage وIndexedDB لتقليل إعادة التنزيل |
| هوية المتجر | شعار دائري، تذييل كامل العرض، شارات متعددة، وإظهار أو إخفاء عناصر قالب اختيارية |
| النص التسويقي | مولد محلي عربي يعمل بلا إنترنت وتنسيق مناسب لواتساب، مع تحسين سحابي اختياري لا يعطل المسار المحلي |
| الدفعات | رفع حتى عشر صور، بيانات مستقلة أو موحدة، توازٍ محدود، حفظ مؤقت، تنظيف واستعادة |
| PWA | قابل للتثبيت، Service Worker، وواجهة تعمل في ظروف اتصال متقطعة بعد حفظ الموارد |
| المطوّر | لوحة محمية لإدارة مزودي AI، الرسائل العامة، رسائل المستخدمين، الإحصائيات، وإيقاف أو تفعيل الحسابات |

## التقنيات المستخدمة

| الطبقة | التقنية | الاستخدام |
|---|---|---|
| واجهة المستخدم | React 19 وTypeScript | شاشات التطبيق والمكونات والحالة التفاعلية |
| التنسيق | Tailwind CSS 4 وshadcn/ui وLucide | هوية الواجهة العربية والمكونات والأيقونات |
| الرسم | HTML5 Canvas API وخط Cairo | إنتاج قالب الإعلان النهائي وتصديره PNG |
| الخلفية | Node.js وExpress وtRPC | إجراءات الخادم، المصادقة، وإدارة مزودي الذكاء الاصطناعي |
| قاعدة البيانات | MySQL/TiDB وDrizzle ORM | المستخدمون، رسائل المستخدمين، الإعلانات العامة، وإعدادات المطوّر |
| المصادقة | Manus OAuth وJWT | تسجيل دخول المستخدم وإدارة الجلسة |
| الذكاء المحلي | ONNX Runtime Web مع U2NetP | إزالة خلفية صور الملابس داخل المتصفح |
| التخزين المحلي | Cache Storage وIndexedDB وlocalStorage | النموذج المحلي، المسودات، دفعات الصور، والإعدادات غير الحساسة |
| الاختبارات | Vitest وTesting Library وjsdom | اختبارات الوحدة والتكامل وسلوك الواجهة |
| البناء | Vite وesbuild وpnpm | التطوير وبناء الإنتاج والتحقق من الميزانية |

## مبدأ العمل بلا إنترنت

بعد التنزيل الأول لموارد التطبيق ونموذج إزالة الخلفية، يهدف التطبيق إلى إبقاء مسار الإعلان الأساسي محلياً: الصورة لا تحتاج إلى مغادرة الهاتف لإزالة الخلفية أو رسم Canvas أو حفظ PNG. يعمل Service Worker مع التخزين المؤقت لملفات الواجهة، ويحفظ IndexedDB مسودات ونتائج الدفعة المؤقتة. وقد يختلف استمرار بعض القدرات باختلاف مساحة تخزين المتصفح وإعدادات الهاتف.

> أي مزود خارجي للذكاء الاصطناعي لا يعد جزءاً من مسار الإنشاء الأساسي؛ هو اختيار للمطوّر فقط ويجب أن يكون مفتاحه محفوظاً في الخادم وليس داخل واجهة المستخدم.

## الجودة والتحقق

كانت آخر نتيجة موثقة للمشروع: **110 اختبار Vitest ناجح**، وفحص TypeScript بلا أخطاء، وبناء إنتاج ناجح. حجم JavaScript المجمّع يقارب **610 KiB** ضمن سقف متابعة قدره **750 KiB**، مع تحميل لوحة المطوّر ومساحة الدفعات والوظائف الثانوية عند الحاجة. تحقق عملياً من واجهة الهاتف على Chromium، بينما اختبار Firefox وSafari قبل أي إطلاق عام يبقى مهمة مستقبلية مستقلة.

## الحماية والخصوصية

لا ينبغي حفظ مفاتيح API في ملفات المشروع أو مشاركتها في المحادثات أو وضعها في LocalStorage. تحفظ لوحة المطوّر حالة وجود المفتاح فقط ولا تعرضه بعد الحفظ. يظل حفظ الصورة وتصديرها محلياً كلما كان ذلك ممكناً، ويجب أن يكون أي إرسال خارجي واضحاً وموافقاً عليه من المستخدم.

## البنية المختصرة للملفات

| المسار | مسؤوليته |
|---|---|
| `client/src/pages/Home.tsx` | رحلة الإنشاء الفردي، المراجعة، النتيجة، والتنقل السفلي |
| `client/src/components/BatchWorkspace.tsx` | رفع الدفعات وبيانات كل صورة وطابور المعالجة |
| `client/src/components/GarmentCropEditor.tsx` | محرر القص الحر قبل تجهيز الصورة |
| `client/src/components/UserTemplateSettings.tsx` | إعدادات المقاس والشعار والتذييل والشارات والعناصر |
| `client/src/lib/canvasRenderer.ts` | الحسابات الهندسية ورسم الإعلان داخل Canvas |
| `client/src/lib/localBackgroundRemoval.ts` | تشغيل نموذج الإزالة المحلي والكاش والاستعادة |
| `shared/marketingText.ts` | مولد النصوص التسويقية المحلية وتنسيق واتساب |
| `client/src/components/DeveloperWorkspace.tsx` | إدارة مزودي AI ومفاتيحهم وحالتهم |
| `client/src/components/DeveloperPersonalConsole.tsx` | الإحصائيات والإعلان العام والرسائل وإدارة الحسابات |
| `server/routers.ts` | إجراءات tRPC للذكاء الاختياري ولوحة المطوّر |

## حالة Try‑On

ميزة تلبيس الملابس على شخص **ليست مفعلة للمستخدم في المسار العادي**. جرى تأجيلها لأنها تحتاج مزوداً فعلياً مرخّصاً، اختباراً عملياً على صور مناسبة، وحدوداً واضحة للوقت والخصوصية والكلفة. المسار المحلي الحالي، وهو إزالة الخلفية ثم وضع القطعة داخل قالب الإعلان، يبقى هو المسار الموثوق والأساسي.

## طريقة العمل على الهاتف

يستطيع المالك تجربة التطبيق من رابط المعاينة، وتثبيته عندما يدعم المتصفح ذلك، والعمل من الهاتف. لحفظ نسخة برمجية خاصة يمكن استخدام GitHub الخاص وTermux، لكن لا تُرفع ملفات البيئة أو مفاتيح API أو ملفات بناء مؤقتة إلى المستودع.

## حدود معروفة ومقترحات قريبة

يعتمد الأداء النهائي لإزالة الخلفية على مواصفات الهاتف والصورة. وقد تختلف مشاركة PNG المباشرة وقراءة الصور من المعرض بين المتصفحات. لذلك يركز التطوير المستقبلي على: تقرير تقدّم أفضل، فحص محلي لجودة الصورة، معالجة دفعات متكيفة، اختبار متصفحات أوسع، وتقارير أخطاء اختيارية وآمنة.

## الخلاصة

المشروع مثال عملي متقدم لتعلم بناء تطبيق ويب حديث باللغة العربية: يجمع بين React وTypeScript وCanvas وPWA والذكاء المحلي والتخزين في الهاتف والخادم الآمن. قيمته الأساسية ليست في عدد الميزات، بل في إبقاء عملية إنشاء إعلان الملابس قابلة للفهم والاستخدام من الهاتف حتى عند ضعف الاتصال.

