# خط أساس إعادة بناء مولد إعلانات الملابس

**التاريخ:** 12 أغسطس 2026  
**الحالة:** وثيقة مرجعية قبل إعادة التصميم المعتمد للهاتف

## الغرض

توثق هذه الصفحة نقطة البدء قبل تحويل التطبيق إلى تجربة عربية مبسطة من ثلاث مراحل: رفع الملابس، إدخال بيانات الإعلان وتوليده، ثم مراجعة الإعلان ومشاركته. لا تتضمن هذه الوثيقة أي مفاتيح API أو كلمات مرور أو بيانات سرية.

## نتائج التحقق الحالية

| الفحص | النتيجة | الملاحظة |
|---|---|---|
| `pnpm test` | ناجح | اختبار المصادقة الحالي نجح: ملف اختبار واحد واختبار واحد. |
| `pnpm check` | ناجح | فحص TypeScript لا يعرض أخطاء. |
| تشغيل واجهة التطوير | عاد للعمل | أصلح تعريف رموز Tailwind 4 واستبدل اختصاراً غير مدعوم؛ ظهرت واجهة الرفع الحالية في المعاينة. |

## المكونات القائمة

| المجال | المكونات والملفات | القرار في إعادة البناء |
|---|---|---|
| مسار الواجهة | `client/src/pages/Home.tsx` و`Stepper.tsx` | إعادة بناء كاملة من أربع خطوات إلى ثلاث مراحل هاتفية. |
| رفع الصورة | `client/src/components/ImageUploader.tsx` | إعادة استخدام منطق الرفع مع تبسيط النص والتخطيط. |
| بيانات الإعلان | `client/src/components/ProductForm.tsx` | إعادة تصميم لتصبح حقولاً اختيارية ومخفية التفاصيل المتقدمة. |
| معاينة وتنزيل الإعلان | `AdPreview.tsx` و`SharePanel.tsx` | تحديثهما للشاشة النهائية وأزرار التعديل والتنزيل والمشاركة وواتساب. |
| قالب الإعلان | `client/src/lib/canvasRenderer.ts` | تحديثه للقالب العربي الثابت، خيارات الإظهار، المقاسات، وحالة الرجوع المحلية. |
| الإعدادات | `SettingsPanel.tsx` | قصرها على خيارات المستخدم الخاصة بعناصر القالب والمقاس. |
| لوحة المطور | `DeveloperPanel.tsx` | فصلها عن الإعدادات وتغيير بوابة الدخول إلى اسم مستخدم وكلمة مرور. |
| البيانات المشتركة | `shared/types.ts` | فصل بيانات الإعلان عن إعدادات القالب وبيانات مزود الذكاء الاصطناعي. |
| التخزين المحلي | `client/src/lib/storage.ts` | الاحتفاظ به للبيانات غير الحساسة فقط، مع منع مفاتيح API من واجهة المستخدم. |
| تكاملات الذكاء الاصطناعي | `client/src/lib/ai.ts` | استبداله بمسار خادمي آمن؛ لا تستخدم مفاتيح في متغيرات واجهة `VITE_*`. |

## معالجة خلل التنسيق الأولي

كان ملف التنسيق يستخدم رموزاً دلالية مثل `bg-card` دون تعريف Tailwind 4 بصيغة `@theme inline`، كما استعمل اختصار `text-overflow-ellipsis` غير متاح في هذا الإعداد. تمت إضافة الرموز بصيغة OKLCH واستبدال الاختصار بـ `truncate`، ثم أُعيد تشغيل خادم التطوير والتحقق من تحميل شاشة رفع المنتج الحالية.

## تحقق بصري أولي

بعد إعادة بناء شاشة البداية، أظهرت معاينة الهاتف رأس الهوية مع شعار Marwan Designer، ومؤشر المراحل الثلاث، وبطاقة رفع كبيرة مناسبة للمس، وشريط تنقل سفلي يضم إنشاء وإعدادات ومطور. لا تزال شاشات الإعدادات والمطور وتفاصيل القالب قيد الانتقال من النموذج القديم إلى التصميم الجديد.

تم أيضاً اختبار انتقال الواجهة باستخدام ملف صورة اختبار: انتقلت الشاشة تلقائياً من رفع الملابس إلى نموذج بيانات الإعلان، وظهرت حقول الاسم والسعر والخصم والكمية والألوان والميزات، مع بقاء التفاصيل الإضافية مطوية افتراضياً.

بعد ملاحظة مهلة في أول محاولة تلقائية لتوليد Canvas، أضيفت حماية لتحجيم صورة الرفع قبل تخزينها، ومهلة لتحميل الصورة، ومهلة عامة للتصدير. بدأ اختبار جديد بصورة صغيرة للتحقق من سلوك النتيجة النهائية دون تعليق الواجهة.

أظهر التحليل اللاحق أن تحويل Canvas إلى Data URL كان يعمل بصورة متزامنة ويزيد ضغط الذاكرة في المتصفح. لذلك حُوّل مسار صورة الملابس ونتيجة الإعلان إلى `Blob URL` غير متزامن مع تنظيف الروابط القديمة، وهو أنسب لواجهة هاتف محدودة الذاكرة.

أعيدت تهيئة معاينة مستقلة وبدأ اختبار رفع صورة صغيرة عبر مسار Blob للتحقق من أن التحسين لا يغيّر انتقال المستخدم بين مراحل التطبيق.

أكدت المعاينة أن صورة الاختبار أصبحت رابط Blob وأن شاشة بيانات الإعلان تظهر بصورة سليمة. بدأ اختبار توليد النتيجة من الجلسة نفسها عبر تشغيل حدث الزر دون انتظار أتمتة النقر، لتفادي حد المهلة الخاص بأداة الاختبار.

اكتمل اختبار النتيجة: ظهرت شاشة الإعلان النهائي مع صورة Canvas، وتنبيه الرجوع المحلي، والنص التسويقي، وأزرار تنزيل PNG والمشاركة وواتساب والتعديل. عولج أيضاً تكرار جملة الدعوة للطلب عند عدم إدخال سعر، ونجحت اختبارات المشروع بعد ذلك.

أثناء التحقق من إعدادات المستخدم، ظهرت نسخة الواجهة القديمة في جلسة متصفح جديدة رغم تحديث عامل الخدمة. تمت زيادة إصدار التخزين المؤقت وإضافة تعطيل صريح لعامل الخدمة في وضع التطوير، ولا يزال التحقق النهائي من إزالة النسخة القديمة جارياً قبل اعتماد النسخة.

نجح التحقق اللاحق: بعد تحميل ثانٍ ظهرت واجهة المراحل الثلاث الحالية، وبعد إعادة تحميل نظيفة بدأ التطبيق من مرحلة رفع الملابس بدلاً من استعادة إعلان بلا صورة. ستظل تفاصيل الإعلان وإعدادات القالب محفوظة، لكن يجب رفع الصورة مجدداً عند بدء جلسة جديدة.

أثناء فحص التبويب السفلي، لم ينتقل اختبار أتمتة النقر إلى شاشة الإعدادات رغم ظهور الواجهة الحالية؛ سيجري التحقق من التفاعل مباشرة قبل اعتماد فصل الإعدادات ولوحة المطور.

نجح التحقق المباشر: تعرض شاشة إعدادات المستخدم مقاس المنشور أو القصة وعناصر الإظهار أو الإخفاء للقالب فقط، مثل الخصم والألوان والسعر ومعلومات المتجر، دون حقول مفاتيح أو إعدادات تقنية.

تظهر لوحة المطور في تبويب مستقل وتعرض بوابة دخول باسم المستخدم وكلمة المرور فقط قبل عرض أي إعدادات لمزودي الذكاء الاصطناعي.

اكتمل اختبار دخول لوحة المطور بالبيانات الخادمية: فتحت جلسة مطور قصيرة العمر عبر كوكي HttpOnly، ثم ظهرت واجهة إدارة المزودين. لا تُظهر الواجهة مفاتيح API المحفوظة؛ تحفظها الخوادم في قاعدة البيانات بعد تشفيرها.

أضيف موصل اختياري لـ FASHN Product to Model وفق عقد الطلب الرسمي، ويستخدمه التطبيق فقط إذا أضاف المطور مزوداً مفعّلاً. بدأت معاينة مستقلة للتحقق من أن عدم وجود مزود لا يمنع المستخدم من إنشاء الإعلان عبر الخطة المحلية.

وصل اختبار الصورة الصغيرة إلى مرحلة بيانات الإعلان، ثم بدأ توليد الإعلان للتحقق من أن استجابة «لا يوجد مزود مفعّل» تؤدي إلى القالب المحلي بدلاً من إيقاف المستخدم.

تمت معالجة خطأ محلي مستقل كان يمنع ترميز النصوص العربية داخل التخزين المحلي. التحقق النهائي في متصفح جديد يعرض شاشة رفع الملابس الحالية دون أخطاء في وحدة التحكم، كما اجتازت اختبارات التخزين العربي ومساري Try-On واختبارات Home التفاعلية وبناء الإنتاج.

أضيف رابط «حول التطبيق وبيانات المطور» داخل إعدادات المستخدم، وتحقق المتصفح من ظهوره مع الحفاظ على شريط التنقل السفلي المكوّن من إنشاء وإعدادات ومطور فقط.

اكتمل التحقق البصري من شاشة حول التطبيق: تظهر هوية Marwan Designer واسم المهندس مروان داغس ورقم 770976559 ومعرّف GitHub `yemenhd3-create` داخل واجهة الهاتف.

تم التحقق من استجابة شاشة رفع الملابس على هاتف ضيق بعرض 320 بكسل وعلى جهاز لوحي بعرض 768 بكسل. ظلت الخطوات والأزرار وشريط التنقل مقروءة وقابلة للوصول، مع الحفاظ على اتجاه RTL.

تمت إضافة لقطة تحقق لهاتف كبير بعرض 390 بكسل ولقطة سطح مكتب بعرض 1280 بكسل. تظهر عناصر رأس الصفحة والـStepper ومنطقة الرفع والتنقل السفلي باتجاه RTL من دون قصّ أو تراكب مرئي في المقاسات المفحوصة.

تم التحقق من مسار الإعدادات في المتصفح: بطاقات المقاس، مفاتيح إظهار عناصر القالب، رابط حول التطبيق، وزر العودة تحافظ على محاذاة واتجاه RTL.

تم التحقق من بوابة المطور في المتصفح: حقول المزود والرابط والنموذج والمفتاح والسجل التشخيصي تظهر بمحاذاة RTL، ويظهر حقل المفتاح من نوع كلمة مرور مع نص يؤكد عدم كشف القيمة المحفوظة.

أظهرت لقطة تحقق آلية واحدة بعرض 390 بكسل انزياحاً أفقياً غير متوقع، بينما أظهرت معاينة المتصفح شاشة الرفع بصورة طبيعية. سُجلت مهمة تحقق وإصلاح احترازية، ولن يعتمد اجتياز الاستجابة النهائي قبل تأكيد لقطة الهاتف بعد تشخيص قياسات DOM.

أضيفت حماية عامة لغلاف `html` و`body` و`#root` لمنع العرض خارج عرض الجهاز مع اعتماد `box-sizing: border-box` وإزالة الهامش الافتراضي للجسم. نجحت لقطة التحقق اللاحقة بعرض 390 بكسل: ظهر الرأس ومؤشر المراحل وبطاقة الرفع كاملة ضمن العرض من دون قص أو انزياح أو تمرير أفقي.

أضيف الأمر `pnpm mobile:check`، وهو فحص Chromium مستقل يفتح الواجهة بعرض **390 بكسل** ويقارن `documentElement.scrollWidth` و`body.scrollWidth` بعرض النافذة، ثم يسجل العناصر المتجاوزة إن وجدت. النتيجة الموثقة كانت: `viewportWidth=390` و`documentWidth=390` و`bodyWidth=390` مع قائمة تجاوز فارغة؛ لذلك لا يوجد تجاوز أفقي فعلي في شاشة الرفع بعد الإصلاح.

عُزّز الفحص ليقيس غلاف المكوّنات المعروضة مباشرة، وليس عرض المستند وحده. كانت حدود `#root` و`header` و`main` و`nav` جميعها من `0` إلى `390` بكسل، مع قائمة عناصر متجاوزة فارغة. بناءً على هذا القياس القابل لإعادة التشغيل، صُنفت لقطة الانزياح الأولى كـ **إنذار كاذب من أداة اللقطة الكاملة** وليست عيباً مرئياً في التخطيط الحالي؛ إذ لا يُخفي `overflow-x` أي عنصر خارج حدود النافذة بحسب فحص المستطيلات الفعلي.

## تحقق الأداء وبوابة الجودة

أضيف الأمر `pnpm quality` ليشغّل اختبارات Vitest وفحص TypeScript وبناء الإنتاج ثم `pnpm perf:check` في تسلسل واحد. كما أضيف فحص قابل لإعادة التشغيل في `scripts/performance-check.mjs` يرفض البناء إذا تجاوزت الحزمة الرئيسية أو حزم التحميل الأولي أو CSS أو الحزم المؤجلة ميزانياتها المحددة.

| المقياس بعد البناء | القيمة الفعلية | الميزانية | النتيجة |
|---|---:|---:|---|
| JavaScript الرئيسي | 156.3 KiB | 180 KiB | ناجح |
| JavaScript للتحميل الأولي (الرئيسية + الموردون) | 696.7 KiB | 750 KiB | ناجح |
| CSS الأولي | 113.1 KiB | 130 KiB | ناجح |
| شاشة «حول التطبيق» المؤجلة | 6.6 KiB | ضمن 45 KiB للحزم المؤجلة | ناجح |
| لوحة المطور المؤجلة | 29.2 KiB | ضمن 45 KiB للحزم المؤجلة | ناجح |

أدى تحميل «حول التطبيق» ولوحة المطور عند الطلب إلى إبقاء **35.8 KiB** من JavaScript خارج تدفق المستخدم الأولي. اجتازت بوابة الجودة في 12 أغسطس 2026: **13 ملف اختبار و29 اختباراً**، وفحص TypeScript، وبناء الإنتاج، وفحص ميزانية الحزم. هذا تحقق بنيوي قابل للتكرار داخل المشروع؛ ولا يُعد بديلاً عن قياس شبكة حقيقية على هاتف المستخدم أو تقرير Lighthouse.

## الحدود المتفق عليها

سيظل التطبيق عملياً عند عدم توفر Try-On؛ يعرض صورة الملابس الأصلية داخل القالب بعد تحجيمها وقصّها بما يلائم المنطقة المخصصة، ثم يبلغ المستخدم بذلك بلغة هادئة. إعدادات المستخدم لا تتضمن مفاتيح أو مزودين أو سجلات تقنية. لوحة المطور وحدها هي التي تدير مزودي الذكاء الاصطناعي ومفاتيح API والاختبارات والسجل التشخيصي.

## نطاق النسخة التالية

تركز النسخة التالية على تجربة الهاتف، القالب، المشاركة، الإعدادات المنفصلة، والحماية الأساسية للمطور. أما مزود Virtual Try-On السحابي الفعلي فينفذ بعد توثيق الخيارات والترخيص والحدود المجانية، مع بقاء الخطة المحلية جاهزة منذ البداية.

## حدود الإصدار التعليمي المعتمد

يُعد هذا الإصدار مكتمل النطاق للتعلم وإنشاء إعلانات محلية من الهاتف مع Try-On اختياري عبر مزود يضيفه المطور. أُرشفت المهام التالية كمرحلة لاحقة وليست عيوباً في الإصدار: عداد الحصص والمزود الاحتياطي، رفع صورة شخص وفئة قطعة، تشغيل CatVTON على GPU، وتقييم 100 زوج من الصور.

لا توجد في هذه النسخة التعليمية حسابات عامة أو استهلاك مدفوع للمزود، لذلك لا معنى لتفعيل **عداد حصص لكل مستخدم** أو **مزود احتياطي تلقائي** قبل اختيار المزود الفعلي وتحديد الرصيد والترخيص وسياسة الاستخدام. عند تحويل التطبيق إلى خدمة عامة، تُنفّذ هذه الحماية على الخادم قبل إتاحة Try-On للمستخدمين. أما التحقق المتوفر حالياً فهو Chromium فقط؛ يظل اختبار Firefox وSafari شرطاً واضحاً قبل أي إطلاق عام واسع.

## إصلاحات الاستخدام من الهاتف — أغسطس 2026

استجابة لمراجعة المستخدم، أصبحت قراءة الصورة تقبل JPG وPNG وWebP مع رسالة عربية مفيدة عند رفض الملف أو تجاوز الحجم، وتستخدم الملف الأصلي كحل آمن إذا تعذر ضغط الصورة في Canvas على بعض أجهزة Android. أضيفت أزرار الرجوع إلى التعديل ومسح جلسة الإعلان بعد تأكيد المستخدم. كما أصبح اسم المتجر ورقم التواصل حقولاً ظاهرة في النموذج، ويظهران في الشريط الأحمر داخل Canvas وفي النص التسويقي المصاحب للمشاركة.

أعيد بناء القالب وفق التخطيط المرجعي: خلفية عاجية وحد داخلي، بطاقة صورة بيضاء بظل خفيف، شارة خصم، بطاقة كمية وألوان، بطاقة سعر حمراء، شارات مزايا، وشريط متجر سفلي. تستخدم صورة المنتج أو PNG الشفاف أسلوب `contain` عالي الجودة داخل البطاقة، لذلك لا تمدد أو تقص. اجتازت التعديلات 35 اختباراً، وفحص TypeScript، وبناء الإنتاج، وفحص الأداء وفحص الهاتف 390 بكسل.

كشف سيناريو هاتف تجريبي أن بعض مسارات صور Android قد تفشل عند فك الملف عبر Blob URL وحده. عولج ذلك بإعادة محاولة منظمة عبر `FileReader` وتحويل الصورة المقبولة إلى Blob ناتج من Canvas قبل بدء مرحلة الإعلان، بدلاً من تمرير مصدر غير قابل للرسم إلى القالب. نجح السيناريو اللاحق في الوصول إلى صورة الإعلان النهائية.

أنتج فحص Canvas التجريبي إعلاناً نهائياً على شاشة هاتف افتراضية بعرض 390 بكسل. أكد الفحص ظهور كل طبقات المرجع: شارة الخصم، عنوان المنتج، البطاقة البيضاء الوسطية، معلومات الكمية والألوان، بطاقة السعر، المزايا، شريط المتجر الأحمر، والنص التسويقي. ظهر أيضاً زر **حفظ PNG في الهاتف** وزر واتساب وزرا تعديل ومسح الجلسة. يغطي اختبار Home الآن مشاركة النظام ومسار واتساب ثم العودة إلى التعديل، ونجح في الحالتين.

يمكن إعادة هذا التحقق أثناء تشغيل خادم التطوير بالأمر `node scripts/canvas-preview-check.mjs`. ينشئ الأمر ملف العينة الخاص به ولا يستخدم أي صورة يرفعها المستخدم، ثم يحفظ لقطة النتيجة في `/home/ubuntu/canvas-preview-check.png` ويعيد حالة نجاح فقط عند ظهور صورة Canvas وزر التعديل. لذلك يبقى تحقق القالب مستقلاً وقابلاً للتكرار.

## المساحة الشخصية والحسابات — أغسطس 2026

أضيفت جداول خادمية للإعلان العام ورسائل المستخدمين، وعمود `isDisabled` لحالة الحساب. يطلب التطبيق الدخول عبر OAuth قبل فتح مولد الإعلان، ولا ينشئ قاعدة لكلمات مرور بريد محلية. يستطيع المطور من لوحته المحمية نشر إعلان عام، قراءة رسائل المستخدمين، رؤية عدد الحسابات وحالة كل حساب، ثم إيقاف أو إعادة تفعيل حساب. تُحفظ إعدادات مزود Try-On في الخادم بشكل مشفر كما كانت، لذلك تتزامن بين الهواتف التي تفتح لوحة المطور نفسها.

أضيف اختبار لطبقة المساحة الشخصية يتحقق من حجب الحساب الموقوف وقراءة الإعلان الفعّال، كما حُدّث فحص الهاتف ليدعم شاشة الدخول الشخصية من دون قبول أي تجاوز أفقي.

أصبح مسار Try-On يدعم مزوداً اختيارياً ثانياً بالاسم `background-remove`. عند تفعيله، يشغّل الخادم Product-to-Model ثم يزيل الخلفية من صورة الشخص الناتجة ويحفظ PNG الشفاف قبل وضعه في قالب الإعلان؛ وعند عدم إضافته أو تعذره، يحافظ التطبيق على صورة Try-On العادية دون تعطيل الإعلان. يغطي اختبار موصل Try-On الحالتين ولا تُخزن المفاتيح في العميل.

## نتيجة بوابة الجودة الأخيرة

في آخر تشغيل مكتمل، نجحت **14 مجموعة اختبار و35 اختباراً**، وفحص TypeScript وبناء الإنتاج. بلغت الحزمة الرئيسية 173.6 KiB ضمن ميزانية 180 KiB، بينما بقيت واجهة المطور المؤجلة مع شاشة حول التطبيق ضمن 38 KiB من ميزانية 45 KiB. كما اجتاز فحص الهاتف بعرض 390 بكسل من دون تجاوز أفقي، سواء ظهرت شاشة الدخول الشخصية أو واجهة الإنشاء بعد المصادقة.
