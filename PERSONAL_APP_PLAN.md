# خطة التوسعة الشخصية الخاصة

## الهدف

يبقى التطبيق مشروعاً شخصياً خاصاً للتعلّم. خصوصية مستودع GitHub تحمي الشيفرة، بينما يحمي **الدخول الإلزامي** و**صلاحيات المطور** الوصول إلى التطبيق والرسائل والإعدادات المشتركة عند استخدامه من عدة هواتف شخصية.

## الدخول والصلاحيات

سيستخدم التطبيق مسار OAuth المجهز في المشروع للدخول. هذا يوفّر هوية موحّدة للمستخدم عبر الأجهزة المتصلة بالحساب نفسه. لا يُخزّن التطبيق كلمة مرور بريد محلية. ستظهر واجهة الدخول باسم «دخول إلى مساحتك الشخصية»؛ وطريقة الدخول الفعلية التي يقدمها بوابة OAuth قد تشمل حساب Google بحسب إعدادات الهوية المتاحة في المنصة.

| الدور | الصلاحيات |
|---|---|
| مستخدم مفعّل | إنشاء إعلان، قراءة إعلان المطور، إرسال رسالة للمطور. |
| مستخدم موقوف | لا يستطيع متابعة واجهة الإنشاء أو إرسال الرسائل حتى يعاد تفعيله. |
| المطور | إدارة مزودي Try-On، تعديل الإعلان العام، عرض الرسائل، إحصاء الحسابات، إيقاف وإعادة تفعيل المستخدمين. |

## البيانات الجديدة

| الكيان | الحقول الأساسية | الغرض |
|---|---|---|
| `users` | `isDisabled` | إيقاف وصول مستخدم بعينه من الخادم. |
| `app_announcements` | النص، حالة الظهور، أوقات الإنشاء والتحديث | رسالة عامة واحدة أو أكثر يحررها المطور وتظهر للمستخدمين المسجلين. |
| `user_messages` | المستخدم المرسل، النص، الحالة، وقت الإنشاء | رسالة قصيرة من المستخدم إلى المطور، مع حالة جديد/مقروء/مؤرشف. |

لا تخزّن قاعدة البيانات صور الملابس أو مفاتيح API داخل رسائل المستخدمين. تبقى الصور في التخزين المخصص عند الحاجة، وتظل مفاتيح المزود مشفرة في جدول مزودي المطور.

## تدفق المستخدم

1. يفتح المستخدم التطبيق ويسجل الدخول عبر بوابة الهوية.
2. يفحص الخادم حالة الحساب؛ المستخدم الموقوف يرى رسالة واضحة ولا يصل إلى الإنشاء.
3. يقرأ الإعلان العام إن كان مفعلاً، ثم ينشئ إعلاناً أو يرسل رسالة قصيرة للمطور.
4. يستطيع المطور من لوحة المطور تعديل الإعلان، قراءة الرسائل، رؤية العدد، وإيقاف/تفعيل الحسابات.

## العلاقة بين الهواتف

بيانات حساب المستخدم والإعلان العام والرسائل وإعدادات مزود Try-On المحفوظة خادمياً مشتركة عند الدخول بالحسابات المصرح بها. أما الصورة التي يختارها المستخدم وبيانات الإعلان المؤقتة فتبقى محلياً على الهاتف إلى أن يمسح جلسة الإعلان. لا يشارك التطبيق صور الملابس تلقائياً بين الأجهزة.

## حدود المرحلة الحالية

سيُبنى الدخول عبر OAuth الجاهز ولا تُبنى منظومة بريد وكلمة مرور مستقلة. كما يبقى التكامل الفعلي مع مزود Try-On مرهوناً بمفتاح مزود تضيفه أنت في لوحة المطور؛ لا يُضاف أي مفتاح في الشيفرة أو إلى GitHub.

## Try-On والنتيجة الشفافة

يتكون المسار الاختياري من مرحلتين خادميتين، ولا يرسل مفتاح API إلى الهاتف. ينشئ مزود `product-to-model` صورة الشخص وهو يرتدي القطعة، ثم — عند إضافة مزود ثانٍ مفعّل باسم العملية `background-remove` — يمرر التطبيق النتيجة إلى إزالة الخلفية ويحفظ PNG الشفاف الناتج قبل وضعه في بطاقة الإعلان. إن لم يضف المطور مزود إزالة الخلفية، يبقى التطبيق صالحاً ويضع صورة Try-On العادية أو صورة الملابس الأصلية في القالب.

وفق وثائق FASHN، تستخدم العمليتان `POST /v1/run` ثم استطلاع الحالة. عملية إزالة الخلفية تقبل رابط الصورة أو Data URI في الحقل `inputs.image`، وتنتج PNG شفافاً؛ كما يمكن طلب `return_base64` لتقليل بقاء النتيجة عند المزود. [1] [2]

## المراجع

[1]: https://docs.fashn.ai/api-reference/product-to-model "FASHN Product to Model Documentation"
[2]: https://docs.fashn.ai/api-reference/background-remove "FASHN Background Remove Documentation"
