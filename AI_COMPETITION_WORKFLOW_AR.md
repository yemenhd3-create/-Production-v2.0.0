# بروتوكول الجولات التنافسية للنماذج

## المصدر الوحيد للحقيقة

يعتمد المشروع مستودع GitHub الخاص على أنه المصدر المركزي للشفرة، مع نقطة استعادة Manus للفحص والاسترجاع. لا يرسل ZIP كامل إلى النماذج في كل جولة. بعد كل دمج ناجح وفحص جودة، يُصدَّر التغيير إلى فرع `main` في المستودع الخاص ويُوسم الإصدار داخل ملف `AI_CONTEXT.md`.

## قاعدة التحديث الإلزامية

بعد كل إضافة أو إصلاح أو تحسين مكتمل، ولو كان صغيراً، تُشغّل فحوص المشروع المناسبة ثم تحفظ نقطة استعادة قبل تحديث المستودع الخاص. لا يرفع أي تغيير تجريبي أو فاشل أو غير مختبر، ولا ترفع مفاتيح API أو كلمات المرور أو `.env` أو `node_modules` أو مخرجات البناء والسجلات المؤقتة. يرافق كل تحديث Commit واضح وملف سياق محدث يذكر الميزات الجديدة والمهام المؤجلة.

## ما يقرأه النموذج قبل بدء الجولة

1. رابط المستودع أو نسخة القراءة منه.
2. الملف `AI_CONTEXT.md` الذي يذكر الإصدار الحالي، الميزات المكتملة، القيود، وآخر قرار هندسي.
3. الملف `todo.md` لمعرفة ما هو مؤجل وما لا ينبغي تكراره.
4. ملف التحدي الحالي فقط.

## بروتوكول الجولة

ينشئ كل نموذج فرعاً أو مساحة عمل منفصلة من آخر commit في `main`. لا يدمج أحد النماذج مباشرة في `main`. يعمل كل متنافس على هدف واحد محدد، ثم يسلّم Patch أو Pull Request مع الاختبارات والتقرير. يراجع المشرف التغييرات، يدمج الأجزاء الآمنة فقط، يشغّل فحوص المشروع، ثم يحدّث `main` و`AI_CONTEXT.md` للجولة التالية.

## التسليم المقبول

التسليم الافتراضي هو `PATCH.diff` وملفات معدلة فقط وتقارير الاختبار والأمان. لا يُطلب ZIP كامل إلا إذا كان النموذج لا يدعم Patch أو Pull Request. لا تقبل تقارير بلا كود، ولا كوداً بلا اختبار، ولا أي سر أو ملف `.env` أو `node_modules`.

## قواعد الوصول والخصوصية

يبقى المستودع خاصاً. لا يحصل أي نموذج على كلمة مرور أو مفتاح أو صلاحية كتابة مباشرة إلى الحساب. إن كان النموذج لا يستطيع قراءة مستودع خاص، يُعطى Snapshot للقراءة فقط أو Patch للجولة السابقة، ولا يُفتح المستودع للعموم بسبب المنافسة.
