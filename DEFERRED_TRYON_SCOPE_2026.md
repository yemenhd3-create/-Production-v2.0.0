# تفويض إعادة فتح مسار Virtual Try-On

**الحالة:** مفتوح للتنفيذ بصورة اختيارية ومحكومة.  
**صاحب القرار:** مالك المشروع.  
**تاريخ القرار:** أغسطس 2026.  
**المرجع السابق:** كان هذا الملف يوثق تأجيل المسار. يحل هذا التفويض محل ذلك التأجيل ولا يحذف أسباب الحماية التي بُني عليها.

## القرار

يُسمح الآن ببناء مسار **Virtual Try-On / Product-to-Model** ضمن رحلة المستخدم، بعد رفع صورة القطعة وقبل إدراجها في قالب الإعلان. الهدف هو معاينة تسويقية اختيارية للقطعة على نموذج افتراضي يختاره المستخدم، وليس ادعاء قياس حقيقي أو تطابق كامل للنتيجة.

لا يُستبدل المسار المحلي القائم، الذي يشمل تجهيز الصورة وإزالة الخلفية محلياً وCanvas والتصدير. يبقى هذا المسار هو الوضع الأساسي دائماً، وتبقى التجربة قابلة للاستخدام دون اتصال أو دون مزود مهيأ.

## حدود التفويض

| المجال | القرار الملزم |
|---|---|
| ظهور الميزة | زر اختياري واضح بعد تجهيز صورة القطعة؛ لا تشغيل تلقائي ولا إلزام للمستخدم. |
| اختيار النموذج | يختار المستخدم فئة النموذج وإعدادات القطعة من قوائم محدودة. لا يستنتج النظام العمر أو الجنس أو أي سمة حساسة من صورة. |
| موافقة الخصوصية | لا ترسل صورة أو إعداداً إلى أي مزود قبل موافقة عربية صريحة، تذكر المزود والبيانات المرسلة وسبب الإرسال. |
| الأسرار | يبقى أي مفتاح على الخادم فقط عبر إدارة أسرار أو سجل مزود مشفّر قائم. لا يظهر في العميل أو Git أو الاختبارات أو ملفات التسليم. |
| النتيجة | تعرض في شاشة مراجعة مستقلة، ولا تدخل Canvas أو الحفظ أو المشاركة إلا بعد اعتماد المستخدم الصريح. |
| عدم التهيئة أو الفشل | عند غياب مزود أو مفتاح، أو رفض الموافقة، أو الإلغاء، أو خطأ الشبكة، أو انتهاء المهلة، يستمر الإعلان المحلي بلا فقدان للصورة الأصلية أو الإعدادات. |
| البيانات | لا تحفظ صورة القطعة أو نتيجة Try-On أو رابطها في `localStorage` أو Design Replay أو سجلات تشخيصية. أي تخزين خادمي مؤقت يجب أن يراجع وفق عقد المزود وسياسة الحذف ويُوثق بوضوح. |
| المزود | يلزم توثيق مزودين على الأقل من وثائقهما الرسمية، واختيار واحد أو Adapter محايد بسبب قابلية الاستمرار والخصوصية والحدود والمهلات والإلغاء، لا لمجرد وجود تجربة قصيرة. |

## البنية الموجودة وصلاحية التعديل

توجد طبقة خادمية أولية في `server/tryOn.ts` تدعم Product-to-Model وإزالة خلفية اختيارية، ومكوّن حالة وملفات اختبار مرتبطة بها. هذه الطبقة نقطة بدء صالحة وليست عقداً جامداً. يجوز تعديلها بالحد الأدنى اللازم إذا أثبتت مراجعة عقد المزود أو اختبارات القبول أن ذلك مطلوب، مع الحفاظ على عزل المفتاح في الخادم وإضافة اختبارات انحدار واضحة.

لا يعني وجود بنية أولية أن مزوداً مفعلاً أو مفتاحاً صالحاً موجود الآن. يجب أن تعمل الواجهة بصدق في حالة «التلبيس الاختياري غير مهيأ» وأن تتيح للمستخدم متابعة الإعلان المحلي. لا يجوز الادعاء بأن اتصالاً حياً أو مزوداً مرخصاً تحقق ما لم يُستخدم إعداد حقيقي مخوّل ويُوثق ذلك دون كشف أي سر.

## نطاق التنفيذ المقبول

ينفذ المساعد الخارجي رحلة هاتفية RTL متكاملة تشمل اختيار النموذج، بوابة الموافقة، الطلب الخادمي typed، حالة انتظار أو إلغاء، شاشة مراجعة، واعتماد النتيجة أو رفضها. عليه استخدام واجهة Canvas الحالية ومحرر الإعلان القائم وسجل التصميم الحالي؛ لا ينشئ Renderer أو Canvas أو Demo منفصلاً، ولا يعيد تنفيذ إزالة الخلفية المحلية.

لا يضيف قاعدة بيانات أو Queue أو migration ما لم تظهر حاجة عقدية واضحة لا يمكن تلبيتها بالبنية الحالية. عند الحاجة، يوثق السبب وخطة الهجرة والتراجع منفصلة ولا يطبق تغييراً إنتاجياً من دون مراجعة المطور الرئيسي.

## معايير القبول قبل الدمج

> لا يندمج أي Patch لمجرد أن الواجهة تظهر. لا يندمج إلا بعد مراجعة `PATCH.diff`، ومطابقة SHA الأساسي، ونجاح الاختبارات، وفحص TypeScript والبناء وميزانية الأداء وفحص الهاتف، ومراجعة الخصوصية والأسرار والمسار المحلي.

يجب أن تغطي الاختبارات، على الأقل، غياب المزود، عدم الموافقة، الإلغاء، المهلة، فشل الشبكة، مدخلات غير صالحة، تكرار الإرسال، نتيجة غير صالحة، نجاح يحتاج اعتماداً صريحاً، وعودة سليمة إلى الإعلان المحلي. تبقى نتيجة Try-On قابلة للرفض في كل الحالات، ولا يدعي التطبيق تطابقاً بنسبة 100% أو دقة مقاس حقيقية.

## سلطة الدمج والتشغيل

للمساعد الخارجي حرية هندسية كاملة **داخل هذا التفويض** للبحث والتصميم وتقديم Patch واختبارات وتوثيق. لا يملك صلاحية دمج أو نشر أو إضافة سر أو تفعيل مزود باسم المالك. تبقى مراجعة الحزمة والدمج وتكوين أي سر وتشغيله الحي من اختصاص المطور الرئيسي والمالك فقط.
