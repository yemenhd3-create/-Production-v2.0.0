# سجل تغييرات المشروع

## 2026-08-16 — Visual Repair Loop v1 للعنوان

أضيفت حلقة إصلاح محلية مغلقة لحالة `BLOCK` المرئية في **عنوان الإعلان فقط**. عند طلب المستخدم الإصلاح، تحفظ النسخة الأصلية من `TemplateSettings` وPNG والتقارير في الذاكرة، وتضبط خلفية العنوان المقروءة، ثم تستعمل `renderAd` الحالي لإنتاج PNG جديد قبل إعادة تشغيل `Pixel Truth` و`DesignContract` و`Quality Gate`. لا تُقبل النتيجة الجديدة ولا يُفتح التصدير إلا عندما تكون النتيجة المعاد رسمها بلا `BLOCK` بصري أو هندسي. إذا بقي `BLOCK`، تُلغى النتيجة المرشحة ويظل الإعلان الأصلي كما هو والتصدير محجوباً. يتيح زر التراجع استعادة الصورة والإعدادات والتقرير السابقين من دون حفظ صورة في سجل التصميم.

### الحدود والتحقق

الإصلاح التلقائي لا يشمل السعر أو التذييل أو الشعار؛ لا تظهر خطة إصلاح تلقائي إلا إذا كانت منطقة `header` هي التي فشلت بصرياً. أضيفت اختبارات للتدفق `BLOCK → Repair → PASS/WARNING`، والتدفق الفاشل `BLOCK → Repair → BLOCK`، والتراجع، والمقاسات الخمسة. اجتازت النسخة 42 ملف Vitest تضم 176 اختباراً، وفحص TypeScript، وبناء الإنتاج، وميزانية الأداء بملف CSS قدره 129.9 KiB من سقف 130 KiB، وفحص الهاتف بعرض 390px. فُتح التطبيق في المتصفح، لكن التحقق الحي داخل الجلسة تعذر لأن CAPTCHA في صفحة الدخول فشل؛ لذلك لا يُدّعى اجتياز تدفق المتصفح الكامل.

## 2026-08-16 — Pixel Truth Gate v1

عُزّزت بوابة جودة التصدير بفحص بصري محلي للناتج النهائي بعد اكتمال Canvas. يقرأ الفحص بكسلات ملف الإعلان المحلي فقط، ويحلل مناطق العنوان والسعر والتذييل المحددة من هندسة القالب، ثم يصنفها إلى `PASS` أو `WARNING` أو `BLOCK` بحسب نسبة التباين المرئية. يحجب `BLOCK` الحفظ والمشاركة عبر نفس مسار التصدير، بينما لا تمنع التحذيرات المستخدم. عند تباين حرج يعرض إصلاحاً حتمياً قابلاً للتراجع يعيد خلفية القالب إلى الأبيض. لا يستخدم أي API أو مكتبة خارجية ولا يرسل الصورة أو البكسلات إلى الشبكة.

### التحقق

أضيفت اختبارات بكسلات مباشرة للنص الأسود فوق الأبيض، والنص الفاتح فوق الفاتح، والخلفية متعددة الألوان، والنص العربي RTL، والمقاسات الخمسة، وغياب الإعلان، ومنع الروابط الشبكية، والحتمية، والإصلاح القابل للتراجع. اجتازت النسخة 168 اختبار Vitest، وفحص TypeScript، وبناء الإنتاج، وميزانية الأداء بملف CSS قدره 129.9 KiB من سقف 130 KiB، وفحص الهاتف بعرض 390px.

## 2026-08-16 — Quality Gate للتصدير المحلي

أضيفت بوابة جودة محلية فوق `DesignContract` و`Design Benchmark` تمنع **الحفظ والمشاركة فقط** عند خطأ هندسي حرج: قيمة رقمية فاسدة، عنصر خارج الإعلان، شعار خارج المنطقة الآمنة، تداخل تذييل مع السعر أو المزايا، سعر مطلوب غير ظاهر، أو تصادم مكتشف بالعقد. أما تحذيرا طول النص العربي وتباين النص المقدّر فيظهران للمراجعة ولا يمنعان المستخدم من التصدير. تُعيد البوابة التقييم قبل كل حفظ أو مشاركة، وتعطّل أزرار المشاركة بعد اكتشاف خطأ حرج، وتعرض الإصلاحات الآمنة التي يقدمها عقد التصميم. لا تُرسل أي صورة أو بيانات متجر أو طلب شبكة.

### التحقق

أضيفت اختبارات للقواعد الحرجة، `NaN` و`Infinity`، الخروج من الحدود، التذييل المتداخل، تحذيرات النص العربي والتباين، حساب الدرجة، وألف حالة حتمية، إضافة إلى اختبار واجهة يثبت تعطيل أزرار الحفظ والمشاركة. اجتازت النسخة 157 اختبار Vitest، وفحص TypeScript، وبناء الإنتاج، وميزانية الأداء بملف CSS قدره 129.9 KiB من سقف 130 KiB، وفحص الهاتف بعرض 390px.

## 2026-08-16 — Design Benchmark & Regression v1

تم تنفيذ محرك قياس محلي حتمي فوق `CompositionScore` و`DesignContract`. يقارن المرشحين الثلاثة بدرجات مفصّلة للقطعة والمناطق الآمنة والتذييل والشعار والتوازن، ويعرض علامات الأفضل إجمالاً والأكثر أماناً والأكثر توازناً وPareto من دون تغيير القالب تلقائياً. عند اعتماد اقتراح، تحفظ بصمة جودة هندسية بلا صورة أو بيانات متجر، ثم يظهر تنبيه عند تراجع لاحق يتجاوز الحدود الموثقة أو عند ظهور مخالفة جديدة. أضيفت سبعة Golden fixtures رقمية واختبار 1000 حالة حتمية. اجتازت النسخة 149 اختباراً، والبناء وميزانية الأداء وفحص 390px.

## 2026-08-16 — اعتماد نطاق Design Benchmark & Regression v1

راجعت اقتراح Local Design Benchmark Engine واعتمدته كاتجاه المرحلة التالية لأنه يضيف مقارنة مرشحين واكتشاف تراجع فوق `CompositionScore` و`DesignContract` الحاليين. قُيد النطاق بدرجات هندسية قابلة للتفسير وPareto بثلاثة محاور وبصمة جودة بلا صور وGolden fixtures رقمية و1000 حالة حتمية. يمنع التسليم النظري أو إعادة تسمية الدرجات القائمة، وأي API أو شبكة أو dependency جديدة.

## 2026-08-16 — Progressive Local Preparation v1

نُفذ نطاق التسريع المحلي المعتمد من Turbo Pipeline من دون إضافة شبكة أو مزود أو تشغيل ONNX بالتوازي. عند اختيار الصورة، ينشئ التطبيق بصمة Canvas محلية قصيرة ويعيد استعمال قياسات التحليل الصغيرة (الجودة والألوان والحدود والقص والتخطيط) عند تطابق الصورة وإصدار المحلل. لا يحفظ Cache الصورة أو URL أو بيانات المتجر، ويحدّ نفسه إلى 12 نتيجة، ويهمل السجل الفاسد تلقائياً. تعرض شاشة المراجعة فرقاً صادقاً بين تحليل جديد وتحليل مستعاد مع زمن الجلسة. اجتازت النسخة 145 اختباراً، وفحص TypeScript والبناء وميزانية الأداء وفحص 390px.

## 2026-08-16 — اعتماد نطاق Progressive Local Preparation v1

راجعت فكرة Turbo Pipeline ولم أقبل تحويلها إلى جدولة متوازية شاملة؛ لأن تشغيل قراءة Pixels وإزالة الخلفية ONNX معاً قد يرفع ذروة ذاكرة الهاتف بلا دليل قياس. اعتمد بدلاً منها نطاق محلي ضيق قابل للقياس: Cache محدود لنتائج تحليل الصورة، بصمة محلية، تقدم صادق، ومقاييس جلسة؛ بلا صورة أو URL أو API أو Worker أو تشغيل ONNX بالتوازي. وثق القرار واختبارات القبول في `TURBO_PIPELINE_PROPOSAL_REVIEW_AR.md`.

## 2026-08-16 — Design Replay v1 وسجل الإصلاح المحلي

تمت مراجعة حزمة Design Replay المرفقة مقابل `a329995` ودمج فكرتها بعد إعادة بناء الأجزاء غير المكتملة. أضيف سجل دلالي محلي بلا صورة أو بيانات متجر أو روابط، يدعم إضافة العملية وإعادة تشغيلها وUndo وRedo وحذف العملية عندما لا يوجد تعارض. يُثبت النظام تطابق وثيقة التصميم عبر بصمة حتمية، لا عبر ادعاء تطابق PNG بين أجهزة أو متصفحات مختلفة. أضيفت واجهة السجل إلى بطاقة عقد التصميم، وصار إصلاح العقد يسجل ويعاد فحصه قبل تطبيقه. اجتازت النسخة 141 اختباراً وفحص TypeScript والبناء وميزانية الأداء وفحص 390px.

### ملاحظة مراجعة الحزمة

لم يُطبق `PATCH.diff` المرفق مباشرة لأنه كان مرجعاً نصياً إلى `changed-files` لا Patch قابلاً للتطبيق، ولم يربط التخزين أو الواجهة أو Undo/Redo الفعلي. احتُفظ بالفكرة المفيدة وأعيد تنفيذها مع كشف تعارض صارم، ومفتاح تخزين محلي، وحذف عملية آمن، واختبار عشرين عملية متتابعة.

## 2026-08-16 — Pilot v1 لعقد التصميم المحلي

تم تنفيذ أول نسخة عملية من «المترجم المحلي للتصميم» من دون API أو رفع صور. صار المشروع يبني وثيقة تصميم حتمية وخالية من الصورة وبيانات المتجر، ويستخدم هندسة موحدة للمقاسات الخمسة في Canvas والتحقق. أضيف فحص عقد في شاشة الإعلان النهائي يختبر حدود العناصر ومنطقة القطعة وسلامة الشعار والتذييل والسعر، ويعرض إصلاحاً اختيارياً قابلاً للتراجع. كشف الاختبار أيضاً قيداً قديماً كان يمد تذييل القصة وواتساب فوق منطقة المزايا، وصححه التنفيذ. اجتازت النسخة 136 اختباراً وفحص TypeScript والبناء وميزانية الأداء وفحص الهاتف 390px.

## 2026-08-16 — اعتماد نطاق «عقد التصميم المحلي»

رُوجع اقتراح «المترجم المحلي للتصميم» واعتمدت فكرته كاتجاه معماري، لا كتسليم برمجي. حُدد Pilot v1 صغير وقابل للاختبار: وثيقة تصميم خالية من الصورة وبيانات المتجر، وعقد هندسي للمقاسات الخمسة، وخطط إصلاح قابلة للتطبيق والتراجع. أُجل Graph solver العام وسجل العمليات والمراجع السحابي إلى ما بعد ثبات هذا النطاق. التفاصيل في `LOCAL_DESIGN_COMPILER_PROPOSAL_REVIEW_AR.md`.

## 2026-08-16 — مراجعة حزمة «الاستوديو الذكي المحلي»

رُفض دمج الحزمة مباشرة بعد فحص ZIP والـPatch والملفات والاختبارات. الـPatch لا يطابق بنية التطبيق، ويكرر المحلل ومولد النص والتخزين القائمين، ويولّد نصوصاً تحتوي ادعاءات غير مدخلة. كما أن QR المرسوم نمط hash وليس QR قابلاً للقراءة، وfallback التخزين يفسد الصور بقص بياناتها، واختبارات الحزمة شكلية. وثقت المراجعة الكاملة وشروط إعادة التسليم في `SMART_LOCAL_STUDIO_PACKAGE_REVIEW_AR.md`.

## 2026-08-16 — مراجعة تسليم «المحرك الإعلاني الذكي الهجين»

رُفض الدمج المباشر لتسليم المحرك الهجين. يكرر مولد النص والتباين والشارات الموجودة، ويحتوي مساراً سحابياً يضع مفتاح المستخدم في المتصفح، ورمز QR خارجياً رغم ادعاء العمل المحلي، وتوليد SVG غير محمي، وتكاملاً واختبارات غير مرفقة أو غير متوافقة مع بنية المشروع. حُصر التحسين الممكن مستقبلاً في تحذير تباين Canvas محلي صغير، ووثقت متطلبات إعادة التسليم في `SMART_HYBRID_ENGINE_RESPONSE_REVIEW_AR.md`.

## 2026-08-16 — مراجعة تسليم «البصمة الذكية»

رُفض الدمج المباشر لتسليم «البصمة الذكية» لأنه غير مكتمل وغير متوافق مع بنية المشروع: اعتمد على مسارات ومكوّنات غير موجودة، وحذف محركاً أساسياً من التسليم، وتضمن Patch تمثيلياً واختبارات لا يمكن تجميعها، كما لا يحل مشكلة تخص إعلان الملابس ويكرر جانباً من المصمم المحلي. وثقت المراجعة ومتطلبات إعادة التسليم في `SMART_FINGERPRINT_RESPONSE_REVIEW_AR.md`.

## 2026-08-16 — إتاحة المراجعة من المستودع العام

تحول المستودع المرجعي إلى قراءة عامة بعد تدقيق ملفات الشجرة وتاريخ Git بحثاً عن ملفات بيئة ومفاتيح خاصة وأنماط مفاتيح شائعة. أضيف بروتوكول مراجعة يطلب من النماذج البدء من آخر `main` وتسليم Patch واختبارات وتقارير فقط، مع بقاء صلاحية الكتابة والدمج محصورة بالمشرف. نُقح README لإزالة رقم الاتصال الشخصي ولتوضيح أن Try-On مؤجل والمسار المحلي هو المعتمد.

## 2026-08-16 — جواز التصميم وفحص الجودة بعد الرسم

أضيف فحص محلي اختياري للناتج النهائي بعد رسم Canvas. يتحقق من أبعاد ملف الإعلان، وتجانس المحتوى البصري، والسطوع العام، ثم ينشئ بصمة SHA-256 وحجم الملف داخل جواز JSON قابل للتنزيل. لا يرسل الفحص الصورة أو بيانات الاتصال أو أي مفتاح، ويرفض صراحة روابط الشبكة كي يبقى على الجهاز. يختفي الجواز تلقائياً عند توليد إعلان جديد أو تغيير الصورة.

### التحقق

أضيفت ستة اختبارات حقيقية لمحرك الجواز تغطي الروابط المحلية، اختلاف الأبعاد، الناتج الأبيض، الناتج الشفاف، المحتوى المتنوع، والبصمة واسم الملف؛ كما توسع اختبار لوحة المشاركة لحالة زر الفحص والانتظار. اجتازت النسخة 36 ملف Vitest تضم 125 اختباراً، وفحص TypeScript، وبناء الإنتاج، وميزانية الأداء، وفحص هاتف بعرض 390px.

## 2026-08-16 — النسخة المرجعية للجولات التنافسية

تم توحيد محرك المصمم المحلي الذكي مع تحسينات آمنة من المناظرة: درجات تكوين حتمية، تفضيلات محلية آمنة قابلة للمسح، مقارنة Canvas قبل/بعد، وقرار اعتماد أو تجاهل مستقل لكل عنصر في الدفعة. تم توثيق حوكمة الجولات والنشر الآمن إلى GitHub، وإضافة ملف سياق ثابت للنماذج المتنافسة.

### التحقق

اجتازت النسخة 118 اختبار Vitest، وفحص TypeScript، وبناء الإنتاج، وميزانية الأداء، وفحص الهاتف بعرض 390px قبل تحديث ملفات التوثيق الحالية.

### مؤجل

المراجع البصري السحابي لا يزال مؤجلاً حتى اختيار مزود رسمي وتنفيذ طبقة متوافقة مع النسخة الحالية ومثبتة باختبارات كاملة.
